<style>
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Montserrat:wght@700;800;900&display=swap');

.gimmie-build-box,
.gimmie-build-box *,
.gimmie-build-box *::before,
.gimmie-build-box *::after {
  box-sizing: border-box;
}

.gimmie-build-box {
  --gimmie-bg: #FBF9F4;
  --gimmie-panel: #FFFFFF;
  --gimmie-text: #102B10;
  --gimmie-accent: #CCE7C2;
  --gimmie-muted: #556B55;
  --gimmie-border: #102B10;
  --gimmie-error: #b42318;
  --gimmie-success: #1E6B24;
  --gimmie-heading-font: 'Montserrat', -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --gimmie-body-font: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  
  background: var(--gimmie-bg) !important;
  color: var(--gimmie-text) !important;
  font-family: var(--gimmie-body-font) !important;
  margin: 0;
  padding: 0;
  min-height: 80vh;
  width: 100%;
}

.gimmie-build-box__section {
  padding: clamp(32px, 5vw, 64px) 20px;
}

.gimmie-build-box__inner {
  max-width: 1360px;
  margin: 0 auto;
}

.gimmie-build-box__header {
  max-width: 850px;
  margin: 0 auto 36px;
  text-align: center;
}

.gimmie-build-box__eyebrow {
  margin: 0 0 10px;
  font-size: 0.88rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--gimmie-muted);
  font-family: var(--gimmie-body-font);
}

.gimmie-build-box__heading {
  margin: 0;
  font-size: clamp(2.4rem, 5.5vw, 4.8rem);
  line-height: 1.02;
  font-weight: 900;
  text-transform: uppercase;
  letter-spacing: -0.02em;
  color: var(--gimmie-text);
  font-family: var(--gimmie-heading-font);
}

.gimmie-build-box__intro {
  max-width: 650px;
  margin: 16px auto 0;
  color: var(--gimmie-muted);
  font-size: 1.05rem;
  line-height: 1.5;
  font-weight: 500;
  font-family: var(--gimmie-body-font);
}

.gimmie-build-box__layout {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 360px;
  gap: 28px;
  align-items: start;
}

.gimmie-build-box__main {
  display: grid;
  gap: 24px;
  min-width: 0;
}

.gimmie-build-box__panel,
.gimmie-build-box__summary {
  border: 2px solid var(--gimmie-border) !important;
  border-radius: 20px !important;
  background: var(--gimmie-panel) !important;
  box-shadow: 4px 4px 0 var(--gimmie-border) !important;
  min-width: 0;
}

.gimmie-build-box__surface {
  padding: clamp(20px, 3.5vw, 28px);
}

.gimmie-build-box__panel-header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 20px;
}

.gimmie-build-box__step {
  width: 38px;
  height: 38px;
  border-radius: 999px;
  border: 2px solid var(--gimmie-border);
  background: var(--gimmie-accent);
  color: var(--gimmie-text);
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--gimmie-heading-font);
  font-weight: 900;
  font-size: 1.15rem;
}

.gimmie-build-box__panel-title {
  margin: 0;
  font-family: var(--gimmie-heading-font);
  font-size: 1.4rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: -0.01em;
}

.gimmie-build-box__details {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 14px;
}

.gimmie-build-box__detail {
  border: 1.5px solid var(--gimmie-border);
  border-radius: 14px;
  padding: 14px 16px;
  background: var(--gimmie-bg);
  font-family: var(--gimmie-body-font);
}

.gimmie-build-box__detail strong {
  display: block;
  font-size: 0.76rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--gimmie-muted);
  margin-bottom: 4px;
}

.gimmie-build-box__detail span {
  font-size: 0.98rem;
  font-weight: 700;
  color: var(--gimmie-text);
}

/* Meals Grid - 2 In a Row */
.gimmie-build-box__meals-grid,
.gimmie-build-box__grid {
  display: grid !important;
  grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
  gap: 22px !important;
}

.gimmie-build-box__meal,
.gimmie-build-box__meal-card {
  border: 2px solid var(--gimmie-border) !important;
  border-radius: 18px !important;
  background: var(--gimmie-panel) !important;
  overflow: hidden !important;
  display: flex !important;
  flex-direction: column !important;
  box-shadow: 3px 3px 0 var(--gimmie-border) !important;
  transition: transform 140ms ease, box-shadow 140ms ease !important;
  position: relative !important;
  min-width: 0 !important;
}

.gimmie-build-box__meal:hover,
.gimmie-build-box__meal-card:hover {
  transform: translateY(-2px) !important;
  box-shadow: 4px 4px 0 var(--gimmie-border) !important;
}

.gimmie-build-box__meal.is-selected,
.gimmie-build-box__meal-card.is-selected {
  border-color: var(--gimmie-success) !important;
  background: #FAFCFA !important;
}

.gimmie-build-box__meal-image-wrap,
.gimmie-build-box__image-wrap {
  position: relative !important;
  width: 100% !important;
  aspect-ratio: 16 / 11 !important;
  background: #EFECE6 !important;
  overflow: hidden !important;
  cursor: pointer !important;
}

.gimmie-build-box__meal-image,
.gimmie-build-box__image {
  width: 100% !important;
  height: 100% !important;
  object-fit: cover !important;
  display: block !important;
}

.gimmie-build-box__info-btn,
.gimmie-build-box__info-trigger {
  position: absolute !important;
  top: 10px !important;
  right: 10px !important;
  width: 32px !important;
  height: 32px !important;
  border-radius: 999px !important;
  border: 1.5px solid var(--gimmie-border) !important;
  background: #FFFFFF !important;
  color: var(--gimmie-text) !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  font-family: var(--gimmie-heading-font) !important;
  font-weight: 900 !important;
  font-size: 0.9rem !important;
  cursor: pointer !important;
  box-shadow: 2px 2px 0 var(--gimmie-border) !important;
  z-index: 2 !important;
}

.gimmie-build-box__info-btn:hover,
.gimmie-build-box__info-trigger:hover {
  background: var(--gimmie-accent) !important;
}

.gimmie-build-box__meal-content {
  display: flex;
  flex: 1 1 auto;
  flex-direction: column;
  padding: 16px;
  font-family: var(--gimmie-body-font);
}

.gimmie-build-box__meal-title {
  margin: 0 0 6px;
  font-family: var(--gimmie-heading-font);
  font-size: 1.15rem;
  font-weight: 800;
  line-height: 1.2;
  text-transform: uppercase;
  cursor: pointer;
  color: var(--gimmie-text);
}

.gimmie-build-box__meal-title:hover {
  color: var(--gimmie-success);
}

.gimmie-build-box__meal-description {
  margin: 0 0 10px;
  color: var(--gimmie-muted);
  font-size: 0.84rem;
  line-height: 1.45;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

/* Clean Macros (No bubbles) */
.gimmie-build-box__meal-macros {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 0.82rem;
  font-weight: 700;
  color: var(--gimmie-text);
  margin-top: auto;
  padding-bottom: 12px;
}

.gimmie-build-box__meal-macros .dot {
  color: var(--gimmie-muted);
  opacity: 0.6;
}

/* Quantity Control Buttons */
.gimmie-build-box__qty {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 14px;
  margin-top: auto;
  padding-top: 12px;
  border-top: 1px solid #ECEAE4;
}

.gimmie-build-box__qty-button {
  width: 40px;
  height: 40px;
  border: 2px solid var(--gimmie-border);
  border-radius: 999px;
  background: var(--gimmie-accent);
  color: var(--gimmie-text);
  font-size: 1.5rem;
  font-weight: 900;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  box-shadow: 2px 2px 0 var(--gimmie-border);
  transition: transform 120ms ease, box-shadow 120ms ease;
  user-select: none;
}

.gimmie-build-box__qty-button:not(:disabled):hover {
  transform: translate(1px, 1px);
  box-shadow: 1px 1px 0 var(--gimmie-border);
}

.gimmie-build-box__qty-button:disabled {
  opacity: 0.35;
  cursor: not-allowed;
  box-shadow: none;
}

.gimmie-build-box__qty-value {
  min-width: 28px;
  text-align: center;
  font-family: var(--gimmie-heading-font);
  font-size: 1.25rem;
  font-weight: 900;
}

/* Summary Sidebar */
.gimmie-build-box__summary {
  position: sticky;
  top: 24px;
}

.gimmie-build-box__progress-bar {
  height: 10px;
  border: 1.5px solid var(--gimmie-border);
  border-radius: 999px;
  background: var(--gimmie-bg);
  overflow: hidden;
  margin: 14px 0;
}

.gimmie-build-box__progress-fill {
  height: 100%;
  background: var(--gimmie-success);
  width: 0%;
  transition: width 200ms ease;
}

.gimmie-build-box__selected-meals {
  display: grid;
  gap: 8px;
  margin-top: 14px;
  max-height: 260px;
  overflow-y: auto;
  padding-right: 4px;
}

.gimmie-build-box__selected-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 8px 12px;
  background: var(--gimmie-bg);
  border: 1.5px solid var(--gimmie-border);
  border-radius: 12px;
  font-size: 0.85rem;
  font-weight: 700;
  font-family: var(--gimmie-body-font);
}

.gimmie-build-box__button {
  width: 100%;
  min-height: 52px;
  margin-top: 18px;
  border: 2px solid var(--gimmie-border);
  border-radius: 999px;
  background: var(--gimmie-text);
  color: #FFFFFF;
  box-shadow: 3px 3px 0 var(--gimmie-border);
  cursor: pointer;
  font-family: var(--gimmie-heading-font);
  font-size: 1rem;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.03em;
  transition: transform 120ms ease, box-shadow 120ms ease, background 120ms ease;
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.gimmie-build-box__button:not(:disabled):hover {
  background: var(--gimmie-success);
  transform: translate(1px, 1px);
  box-shadow: 2px 2px 0 var(--gimmie-border);
}

.gimmie-build-box__button:disabled {
  opacity: 0.4;
  cursor: not-allowed;
  box-shadow: none;
  background: #A0A8A0;
}

/* Nutrition & Ingredients Pop-out Modal (Matching Main Menu Style) */
.gimmie-modal {
  position: fixed;
  inset: 0;
  z-index: 9999;
  display: none;
  align-items: center;
  justify-content: center;
  padding: 20px;
}

.gimmie-modal.is-open {
  display: flex;
}

.gimmie-modal__backdrop {
  position: absolute;
  inset: 0;
  background: rgba(16, 43, 16, 0.65);
  backdrop-filter: blur(4px);
}

.gimmie-modal__content {
  position: relative;
  background: #FFFFFF;
  border: 2.5px solid var(--gimmie-border);
  border-radius: 24px;
  max-width: 620px;
  width: 100%;
  max-height: 90vh;
  overflow-y: auto;
  box-shadow: 6px 6px 0 var(--gimmie-border);
  padding: 28px;
  font-family: var(--gimmie-body-font);
  z-index: 10;
}

.gimmie-modal__close {
  position: absolute;
  top: 18px;
  right: 18px;
  width: 36px;
  height: 36px;
  border-radius: 999px;
  border: 2px solid var(--gimmie-border);
  background: var(--gimmie-bg);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.2rem;
  font-weight: 900;
  cursor: pointer;
  transition: background 120ms ease;
}

.gimmie-modal__close:hover {
  background: var(--gimmie-accent);
}

.gimmie-modal__header {
  margin-bottom: 20px;
}

.gimmie-modal__title {
  margin: 0 0 8px;
  font-family: var(--gimmie-heading-font);
  font-size: 1.6rem;
  font-weight: 900;
  text-transform: uppercase;
  color: var(--gimmie-text);
}

.gimmie-modal__desc {
  margin: 0;
  color: var(--gimmie-muted);
  font-size: 0.95rem;
  line-height: 1.5;
}

.gimmie-modal__section-title {
  font-family: var(--gimmie-heading-font);
  font-size: 1.05rem;
  font-weight: 800;
  text-transform: uppercase;
  margin: 20px 0 8px;
  color: var(--gimmie-text);
  border-bottom: 1.5px solid var(--gimmie-border);
  padding-bottom: 4px;
}

.gimmie-modal__ingredients {
  font-size: 0.88rem;
  line-height: 1.55;
  color: #333333;
}

.gimmie-modal__ingredients strong {
  color: var(--gimmie-text);
  text-decoration: underline;
}

.gimmie-modal__table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 10px;
  font-size: 0.88rem;
}

.gimmie-modal__table th,
.gimmie-modal__table td {
  padding: 8px 10px;
  border-bottom: 1px solid #ECEAE4;
  text-align: left;
}

.gimmie-modal__table th {
  background: var(--gimmie-bg);
  font-weight: 700;
  text-transform: uppercase;
  font-size: 0.78rem;
  color: var(--gimmie-muted);
}

@media (max-width: 900px) {
  .gimmie-build-box__layout {
    grid-template-columns: 1fr;
  }
  .gimmie-build-box__summary {
    position: static;
  }
}
</style>
